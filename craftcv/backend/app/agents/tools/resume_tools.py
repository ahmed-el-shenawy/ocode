from app.repositories.resume_repo import ResumeRepository


async def get_section_info(resume_id: str, section_key: str) -> dict | None:
    pass


async def update_section(resume_id: str, section_key: str, data: dict) -> bool:
    pass
